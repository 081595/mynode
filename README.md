# mynode

## Codespaces setup

This repository installs OpenSpec as a project development dependency.

After opening the repository in GitHub Codespaces, the dev container runs `npm install` automatically during container creation or rebuild, so `npx openspec --version` works without a separate global install.

### Useful commands

- `npm start`
- `npx openspec --version`
- `npm run openspec -- --version`