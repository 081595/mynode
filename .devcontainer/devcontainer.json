{
  "name": "mynode",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:1-22-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/dotnet:2": {
      "version": "8.0",
      "runtime": "aspnetcore"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-dotnettools.csharp",
        "GitHub.copilot-chat",
        "atman-dev.openspec-for-copilot"
      ]
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y sqlite3 libsqlite3-dev && sudo npm install -g @fission-ai/openspec && sudo ln -sf $(command -v openspec) /usr/local/bin/opsx && npm install"
}